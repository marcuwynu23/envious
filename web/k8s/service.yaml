apiVersion: v1
kind: Service
metadata:
  name: envious-web
  namespace: envious
  labels:
    app.kubernetes.io/name: envious
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
  selector:
    app.kubernetes.io/name: envious
